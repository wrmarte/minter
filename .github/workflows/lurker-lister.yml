name: Lurker Lister (OpenSea -> Inbox)

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch: {}

jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Quick sanity (show files)
        run: |
          node -v
          ls -la
          ls -la scripts || true
          ls -la services || true

      - name: Install deps
        run: npm install

      - name: Run lister
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          OPENSEA_API_KEY: ${{ secrets.OPENSEA_API_KEY }}
          LISTER_DEBUG: "1"
          LISTER_LIMIT: "25"
        run: node scripts/lurkerListerAction.js
